import React from 'react';
import Card from '../components/Card';

const AIDetection = () => (
  <div className="container">
    <h2>AI Detection & Monitoring</h2>
    <Card>
      <p>Overview of AI features: anomaly detection, crowd density analysis, object detection on camera feeds, suspicious behavior scoring and automated alerting.</p>
    </Card>
  </div>
);

export default AIDetection;
